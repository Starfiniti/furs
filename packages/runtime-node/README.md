# @starfiniti/furs-runtime-node

Node/Linux runtime composition for file-mounted PKCS#12 secrets, pinned schemas,
strict FURS trust anchors, PostgreSQL and certificate/clock health. Secret values
are read from protected files and are never included in status snapshots.
